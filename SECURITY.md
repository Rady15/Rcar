# Security

Secrets are environment-only and are not shipped in the release archive. Rotate any credential that was previously exposed. Authentication uses HttpOnly SameSite cookies, server-side sessions, password hashing and rate limits.
