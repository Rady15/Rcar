-- Intentionally non-destructive placeholder.
-- Production data must be provisioned through `npm run seed` only in an explicitly approved non-live environment.
-- The application startup seeds public catalog data only when SEED_CATALOG=true and never customer/booking PII by default.
SELECT 'Use the application seed runner for controlled seeding.' AS message;
